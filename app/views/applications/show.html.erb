<h1> <%= @application.name %> </h1>

<div class="row">
    <div class="col-md-8">
        <p> <%= @application.url %> </p>
    </div>

    <div class="col-md-4">
        <%= link_to "De-Register Application", @application, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure?'} %>
    </div>
</div>
